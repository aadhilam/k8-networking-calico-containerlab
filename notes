scp -i ~/.ssh/id_rsa -r /Users/aadhilamajeed/k824/container-labs/containerlab ubuntu@54.198.62.31:/home/ubuntu/ 

scp -i ~/.ssh/id_rsa -r /Users/aadhilamajeed/k824/container-labs/containerlab/calico-lab-1 ubuntu@54.198.62.31:/home/ubuntu/containerlab/
docker import cEOS64-lab-4.34.0F.tar.xz ceos:4.34.0F

sudo iptables -t nat -A PREROUTING -p tcp --dport 8080 -j DNAT --to-destination 172.18.0.3:30081
sudo iptables -t nat -A POSTROUTING -j MASQUERADE

docker exec -it clab-calico-lab-1-k01-control-plane netstat -tulpn | grep 30081


sudo iptables -t nat -A PREROUTING -p tcp --dport 30081 -j DNAT --to-destination 172.18.0.3:30081
sudo iptables -t nat -A POSTROUTING -p tcp -d 172.18.0.3 --dport 30081 -j MASQUERADE

sudo docker inspect k01-control-plane | grep IPAddress

ssh -i ~/.ssh/id_rsa ubuntu@54.198.62.31

apiVersion: v1
kind: Namespace
metadata:
  name: nginx-test
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  namespace: nginx-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: nginx-nodeport
  namespace: nginx-test
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30081  # Choose any port in range 30000–32767


scp -i ~/.ssh/id_rsa -r /Users/aadhilamajeed/k824/container-labs/containerlab/test-k3s-multus ubuntu@$(cat ec2_ip.txt):/home/ubuntu/containerlab/ 