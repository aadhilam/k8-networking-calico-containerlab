! Arista cEOS configuration for VLAN 100 and VLAN 200
! Apply these commands to add VLANs 100 and 200 to your existing switch
!
! To apply: SSH to ceos01 and paste these commands in configure mode
! Or update the startup-config file and redeploy
!
! ============================================
! VLAN Definitions
! ============================================
!
vlan 100
   name Pod-Network-100
!
vlan 200
   name Pod-Network-200
!
! ============================================
! Update trunk interfaces to allow VLANs 100 and 200
! ============================================
!
interface Ethernet1
   description Connection to k01-control-plane (Trunk)
   mtu 9124
   switchport mode trunk
   switchport trunk allowed vlan add 100,200
!
interface Ethernet2
   description Connection to k01-worker (Trunk)
   mtu 9124
   switchport mode trunk
   switchport trunk allowed vlan add 100,200
!
interface Ethernet3
   description Connection to k01-worker2 (Trunk)
   mtu 9124
   switchport mode trunk
   switchport trunk allowed vlan add 100,200
!
! ============================================
! Layer 3 interfaces (SVIs) for VLAN 100 and 200
! ============================================
!
interface Vlan100
   description Pod Network VLAN 100 - L3 Gateway
   mtu 9000
   ip address 10.10.100.1/24
   no shutdown
!
interface Vlan200
   description Pod Network VLAN 200 - L3 Gateway
   mtu 9000
   ip address 10.10.200.1/24
   no shutdown
!
! ============================================
! Update BGP to advertise new networks (optional)
! ============================================
!
router bgp 65010
   address-family ipv4
      network 10.10.100.0/24
      network 10.10.200.0/24
!
end




